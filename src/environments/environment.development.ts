export const environment = {
  production: false,
  apiUrl: 'http://127.0.0.1:8000/api/v1',
  /**
   * Used on localhost (no subdomain) for workshop API calls.
   * Set to your tenant id, e.g. workshop1 — or use subdomain: http://workshop1.localhost:4200
   */
  defaultTenantKey: 'workshop1',
};
