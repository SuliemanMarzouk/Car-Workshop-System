export const environment = {
  production: true,
  apiUrl: 'http://127.0.0.1:8000/api/v1',
  /** Optional fallback when hostname has no subdomain (usually empty in production). */
  defaultTenantKey: '',
};
